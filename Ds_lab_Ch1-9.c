#include <stdio.h>

int main()
{
    char s1[10], s2[10], s3[10];
    int i, j, n;

    printf("Enter the ");

    for (i = 0; i < n;i++)
    {
        printf("Enter The string ");
        scanf("%d", &s1);
    }

    return 0;
}